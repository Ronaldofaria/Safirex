@model Safirex.Models.ClassificacaoOrigem.Esfera

@{
    ViewBag.Title = "Cadastros - Consulta de Esfera";
}
<h4>@ViewBag.Title</h4>
<hr />
<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Nome)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Nome)
        </dd>

    </dl>
</div>

@using (Html.BeginForm())
{
    <hr />
    <div class="col-md-offset-2">
        <button formaction="@Url.Action("Index", "Esferas")" type="submit" data-toggle="tooltip" data-placement="bottom" title='Cancela a consulta do registro e retorna.' class="btn btn-warning btn-lg glyphicon glyphicon-remove"></button>
    </div>
}